## v0.2.16

这次更新让设置、教程与会话切换更清楚、更稳定：

- 自定义后缀改为明确的原生表单，不再把示例误看成固定内容。
- 重新校准 VisiData 3.3 教程按键，帮助、搜索、排序、绘图与分析步骤均按真实行为说明。
- 统一“同时按、依次按、任选”的按键记号，并避免跳过尚未完成的步骤。
- 每次练习都会启动干净会话，教程样本不再进入最近文件。
- 修复延迟启动错误被静默忽略、旧请求误触发新会话的边界情况。
- 快速切换文件时，新会话输出不再等待旧会话积压。
- 串行化进程与时序测试，消除 GitHub Actions 偶发失败。
- 无证书构建也会完整签名并校验应用包，避免嵌入组件残留失效签名。
