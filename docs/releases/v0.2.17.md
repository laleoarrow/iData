## v0.2.17

这是一个启动修复版本，不改变现有 UI：

- 修复 v0.2.16 因主程序与 Sparkle 的 Team ID 校验冲突而在启动时退出的问题。
- 无证书构建不再错误启用 Hardened Runtime；Developer ID 构建仍保持完整运行时保护与时间戳。
- CI 新增最终应用包的真实启动冒烟测试，避免“静态签名通过、实际无法启动”再次漏检。

已安装 v0.2.16 的用户需要从 GitHub 或 Homebrew 手动安装本版本；旧版会在 Sparkle 启动前退出，无法自行更新。
